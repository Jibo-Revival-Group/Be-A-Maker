/* Minimal jQuery stub — BAM playground only used $ in unused populateMedia(). */
(function (global) {
  function jq() {
    return {
      getJSON: function () {
        return { done: function () { return this; } };
      }
    };
  }
  jq.fn = {};
  global.jQuery = global.$ = jq;
})(typeof window !== 'undefined' ? window : global);
